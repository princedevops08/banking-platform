---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.accountService.deploymentname }}
  namespace: {{ .Values.namespace }}
  labels:
    app: account-service
    app.kubernetes.io/part-of: banking-platform
spec:
  replicas: {{ .Values.accountService.replicas }}
  selector:
    matchLabels:
      app: account-service
  template:
    metadata:
      labels:
        app: account-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.accountService.port }}"
        prometheus.io/path: "/metrics"
    spec:
      # Disable legacy service env vars (e.g. POSTGRES_PORT=tcp://ip:5432) that
      # would collide with our typed config env vars.
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
      containers:
        - name: account-service
          image: {{ .Values.accountService.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.accountService.port }}
            - name: grpc
              containerPort: {{ .Values.accountService.grpcPort }}
          envFrom:
            - configMapRef:
                name: account-service-config
            - secretRef:
                name: account-service-secret
          resources:
            requests:
              cpu: 25m
              memory: 48Mi
            limits:
              cpu: "1"
              memory: 192Mi
          readinessProbe:
            httpGet: { path: /readyz, port: {{ .Values.accountService.port }} }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: {{ .Values.accountService.port }} }
            initialDelaySeconds: 10
            periodSeconds: 15
          startupProbe:
            httpGet: { path: /startupz, port: {{ .Values.accountService.port }} }
            failureThreshold: 40
            periodSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.accountService.servicename }}
  namespace: {{ .Values.namespace }}
  labels:
    app: account-service
    app.kubernetes.io/part-of: banking-platform
spec:
  type: ClusterIP
  selector:
    app: account-service
  ports:
    - name: http
      port: {{ .Values.accountService.port }}
      targetPort: {{ .Values.accountService.port }}
    - name: grpc
      port: {{ .Values.accountService.grpcPort }}
      targetPort: {{ .Values.accountService.grpcPort }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: account-service-config
  namespace: {{ .Values.namespace }}
  labels:
    app: account-service
data:
  SERVICE_NAME: "account-service"
  ENVIRONMENT: {{ .Values.env.environment | quote }}
  HTTP_PORT: "{{ .Values.accountService.port }}"
  LOG_LEVEL: {{ .Values.env.logLevel | quote }}
  LOG_FORMAT: {{ .Values.env.logFormat | quote }}
  POSTGRES_ENABLED: "true"
  POSTGRES_HOST: {{ .Values.db.host | quote }}
  POSTGRES_PORT: {{ .Values.db.port | quote }}
  POSTGRES_DB: {{ .Values.db.name | quote }}
  POSTGRES_SSLMODE: {{ .Values.db.sslmode | quote }}
  REDIS_ENABLED: "true"
  REDIS_ADDR: "{{ .Values.redis.servicename }}:{{ .Values.redis.port }}"
  KAFKA_ENABLED: "true"
  KAFKA_BROKERS: {{ .Values.kafka.brokers | quote }}
  OTEL_ENABLED: {{ .Values.otel.enabled | quote }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ .Values.otel.endpoint | quote }}
  OTEL_EXPORTER_OTLP_INSECURE: {{ .Values.otel.insecure | quote }}
  GRPC_ENABLED: "true"
  GRPC_PORT: "{{ .Values.accountService.grpcPort }}"
  LEDGER_GRPC_ADDR: "ledger-service:{{ .Values.ledgerService.grpcPort }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: account-service-secret
  namespace: {{ .Values.namespace }}
  labels:
    app: account-service
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.secrets.dbUser | quote }}
  POSTGRES_PASSWORD: {{ .Values.secrets.dbPassword | quote }}
  JWT_SECRET: {{ .Values.secrets.jwtSecret | quote }}
  JWT_ISSUER: {{ .Values.secrets.jwtIssuer | quote }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.accountService.hpa }}
  namespace: {{ .Values.namespace }}
  labels:
    app: account-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.accountService.deploymentname }}
  minReplicas: {{ .Values.accountService.minReplicas }}
  maxReplicas: {{ .Values.accountService.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
