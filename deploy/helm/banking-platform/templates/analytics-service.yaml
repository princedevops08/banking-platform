---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.analyticsService.deploymentname }}
  namespace: {{ .Values.namespace }}
  labels:
    app: analytics-service
    app.kubernetes.io/part-of: banking-platform
spec:
  replicas: {{ .Values.analyticsService.replicas }}
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.analyticsService.port }}"
        prometheus.io/path: "/metrics"
    spec:
      # Disable legacy service env vars (e.g. POSTGRES_PORT=tcp://ip:5432) that
      # would collide with our typed config env vars.
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
      containers:
        - name: analytics-service
          image: {{ .Values.analyticsService.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.analyticsService.port }}
          envFrom:
            - configMapRef:
                name: analytics-service-config
            - secretRef:
                name: analytics-service-secret
          resources:
            requests:
              cpu: 25m
              memory: 48Mi
            limits:
              cpu: "1"
              memory: 192Mi
          readinessProbe:
            httpGet: { path: /readyz, port: {{ .Values.analyticsService.port }} }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: {{ .Values.analyticsService.port }} }
            initialDelaySeconds: 10
            periodSeconds: 15
          startupProbe:
            httpGet: { path: /startupz, port: {{ .Values.analyticsService.port }} }
            failureThreshold: 40
            periodSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.analyticsService.servicename }}
  namespace: {{ .Values.namespace }}
  labels:
    app: analytics-service
    app.kubernetes.io/part-of: banking-platform
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - name: http
      port: {{ .Values.analyticsService.port }}
      targetPort: {{ .Values.analyticsService.port }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-service-config
  namespace: {{ .Values.namespace }}
  labels:
    app: analytics-service
data:
  SERVICE_NAME: "analytics-service"
  ENVIRONMENT: {{ .Values.env.environment | quote }}
  HTTP_PORT: "{{ .Values.analyticsService.port }}"
  LOG_LEVEL: {{ .Values.env.logLevel | quote }}
  LOG_FORMAT: {{ .Values.env.logFormat | quote }}
  POSTGRES_ENABLED: "true"
  POSTGRES_HOST: {{ .Values.db.host | quote }}
  POSTGRES_PORT: {{ .Values.db.port | quote }}
  POSTGRES_DB: {{ .Values.db.name | quote }}
  POSTGRES_SSLMODE: {{ .Values.db.sslmode | quote }}
  REDIS_ENABLED: "true"
  REDIS_ADDR: "{{ .Values.redis.servicename }}:{{ .Values.redis.port }}"
  KAFKA_ENABLED: "true"
  KAFKA_BROKERS: {{ .Values.kafka.brokers | quote }}
  OTEL_ENABLED: {{ .Values.otel.enabled | quote }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ .Values.otel.endpoint | quote }}
  OTEL_EXPORTER_OTLP_INSECURE: {{ .Values.otel.insecure | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: analytics-service-secret
  namespace: {{ .Values.namespace }}
  labels:
    app: analytics-service
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.secrets.dbUser | quote }}
  POSTGRES_PASSWORD: {{ .Values.secrets.dbPassword | quote }}
  JWT_SECRET: {{ .Values.secrets.jwtSecret | quote }}
  JWT_ISSUER: {{ .Values.secrets.jwtIssuer | quote }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.analyticsService.hpa }}
  namespace: {{ .Values.namespace }}
  labels:
    app: analytics-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.analyticsService.deploymentname }}
  minReplicas: {{ .Values.analyticsService.minReplicas }}
  maxReplicas: {{ .Values.analyticsService.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
