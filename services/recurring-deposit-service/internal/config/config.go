// Package config holds recurring-deposit-service configuration.
package config

import platform "banking-platform/pkg/config"

// Config embeds the shared platform Base.
type Config struct {
	platform.Base
}
